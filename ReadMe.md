1. git init
2. git add .
3. git commit -m "Initial commit"
4. git branch -M main
5. git remote add origin https://github.com/USERNAME/REPO.git
6. git push -u origin main

For the next Time:-
7. git push
8. git push origin main --force

how to install Next.js
- npx create-next-app@latest

Run :  npm run dev == Tailwind automatically import

Next Js Notes:
1. There are two pages always. One is page and other one is layout. in page whatever, you write it will show to only single pay but in layout whatever you write it will show it to all pages like Navbar, Footer and all.

2. As you create folder, the routes will automatically created. == You can create folder with any name but inside you must have to write page.tsx. 

3. You can copy layout content from first one. and layout can see to all pages. You can write pages and layout in only app foldder.

4. Create Utils and Data folder.. In data Assume API data will call here.

5. http://ui.aceternity.com/ => Free components, css, footer,navbar an all. 
npm i motion clsx tailwind-merge Navbar menu search for. and copy manual dependencies and install.

6. "use client"; Write this on any page at the top if you want too use React code